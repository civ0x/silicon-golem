[project]
name = "silicon-golem"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic",
    "websockets",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["golem/test"]
